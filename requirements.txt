# 桌面 AI 陪伴项目 - 依赖清单

# GUI 框架
PyQt6>=6.6.0
PyQt6-Qt6>=6.6.0

# HTTP 请求
requests>=2.31.0
aiohttp>=3.9.0

# 本地数据库
# sqlite3 - Python 内置，无需安装

# JSON 处理
orjson>=3.9.0  # 更快的 JSON 解析

# 配置管理
pydantic>=2.5.0
pydantic-settings>=2.1.0

# 日志
loguru>=0.7.0

# 异步支持
# asyncio - Python 内置，无需安装
aiofiles>=23.2.0

# 图像处理 (可选，用于角色图片)
Pillow>=10.0.0

# 系统托盘
pystray>=0.19.0

# 开发工具
pytest>=7.4.0
pytest-asyncio>=0.21.0
black>=23.0.0
flake8>=6.0.0

# 打包 (发布时用)
pyinstaller>=6.0.0
